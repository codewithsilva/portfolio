# **PORTFOLIO (Next + TypeScript + TSX = Touchdown)**

## **Installation**

```bash
#enough to install and launch the project
npm i

npm run dev

#to generate the basic component
#always let a line after the final export 
`rafc`

# build:
npm run build

npm start
```

> **"Life Starts Now - Three Days Graces"**  
